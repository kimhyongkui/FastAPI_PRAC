from fastapi import FastAPI

app = FastAPI()

city = ["서울", "인천", "대구", "부산", "춘천", "대전"]
print(city)
for i in city:
    print(i)

def city():
    return result
print(city)


# 함수를 만든다
# 함수속에 리스트를 만든다
# 반복문을 만든다
# 반복문에 나온 결과값을 리스트에 추가시킨다
# 함수의 리턴값에 추가된 리스트를 불러온다

